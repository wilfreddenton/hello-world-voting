{
  "name": "hello-world-voting",
  "version": "1.0.0",
  "description": "A simple hello world voting distributed application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "web3": "^0.18.4"
  }
}
